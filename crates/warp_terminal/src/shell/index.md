# crates/warp_terminal/src/shell — index

| File | Holds |
| --- | --- |
| `ShellType.swift` | recognising zsh, bash, fish or something else from an executable path |

Also the list of dotfiles zsh reads out of `ZDOTDIR`, which is what the bootstrap has to shim.
